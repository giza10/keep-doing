<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/button_prev"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="@string/btn_month_prev" />

    <Button
        android:id="@+id/button_next"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="@string/btn_month_next" />

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/button_next"
        android:layout_alignBottom="@+id/button_next"
        android:layout_centerHorizontal="true"
        android:textSize="24sp" />

    <ViewFlipper
        android:id="@+id/flipper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:inAnimation="@anim/calendar_fade_in"
        android:outAnimation="@anim/calendar_fade_out"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/button_prev"
        android:orientation="vertical" >
        <include
            android:id="@+id/page1"
            layout="@layout/calendar_sub_page" />
        <include
            android:id="@+id/page2"
            layout="@layout/calendar_sub_page" />
    </ViewFlipper>
</RelativeLayout>
